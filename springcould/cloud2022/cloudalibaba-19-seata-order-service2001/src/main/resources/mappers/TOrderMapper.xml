<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lquan.mapper.OrderMapper">
    
    <resultMap type="Order" id="OrderResult">
               <result property="id" column="id" jdbcType="INTEGER"/>
               <result property="userId" column="user_id" jdbcType="INTEGER"/>
               <result property="productId" column="product_id" jdbcType="INTEGER"/>
               <result property="count" column="count" jdbcType="INTEGER"/>
               <result property="money" column="money" jdbcType="VARCHAR"/>
               <result property="status" column="status" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="selectOrderVo">
        select id, user_id, product_id, count, money, status from Order
    </sql>

    <select id="selectOrderList" parameterType="Order" resultMap="OrderResult">
        <include refid="selectOrderVo"/>
        <where>  
        </where>
    </select>
    
    <select id="selectOrderById" parameterType="${pkColumn.javaType}" resultMap="OrderResult">
    </select>
        
    <insert id="insertOrder" parameterType="Order">
        insert into Order
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="$column.javaField != null ">$column.columnName,</if>
            <if test="$column.javaField != null ">$column.columnName,</if>
            <if test="$column.javaField != null ">$column.columnName,</if>
            <if test="$column.javaField != null ">$column.columnName,</if>
            <if test="$column.javaField != null ">$column.columnName,</if>
            <if test="$column.javaField != null ">$column.columnName,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="$column.javaField != null ">#{$column.javaField},</if>
            <if test="$column.javaField != null ">#{$column.javaField},</if>
            <if test="$column.javaField != null ">#{$column.javaField},</if>
            <if test="$column.javaField != null ">#{$column.javaField},</if>
            <if test="$column.javaField != null ">#{$column.javaField},</if>
            <if test="$column.javaField != null ">#{$column.javaField},</if>
         </trim>
    </insert>

    <update id="updateOrder" parameterType="Order">
        update Order
        <trim prefix="SET" suffixOverrides=",">
        </trim>
        where ${pkColumn.columnName} = #{${pkColumn.javaField}}
    </update>

    <delete id="deleteOrderById" parameterType="${pkColumn.javaType}">
        delete from Order where ${pkColumn.columnName} = #{${pkColumn.javaField}}
    </delete>

    <delete id="deleteOrderByIds" parameterType="String">
        delete from Order where ${pkColumn.columnName} in 
        <foreach item="${pkColumn.javaField}" collection="array" open="(" separator="," close=")">
            #{${pkColumn.javaField}}
        </foreach>
    </delete>
    
</mapper>
